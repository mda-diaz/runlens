[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "runlens-sdk"
version = "0.1.0"
description = "Debug and compare AI agent runs"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
license = { text = "MIT" }

[[project.authors]]
name = "RunLens"

[tool.setuptools.packages.find]
where = ["."]
