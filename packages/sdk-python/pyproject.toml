[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "runlens"
version = "0.1.0"
description = "Debugging and optimization SDK for AI agents"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
api = ["requests>=2.28"]

[tool.setuptools.packages.find]
where = ["."]
